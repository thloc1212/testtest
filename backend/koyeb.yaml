# Koyeb Configuration
app: thera-backend
services:
  web:
    build:
      buildpack: python
    run: uvicorn main:app --host 0.0.0.0 --port $PORT
    ports:
      - port: 8000
        protocol: http
    env:
      - PORT=8000
    instance_type: free
    regions:
      - sin
